#!/bin/bash

unalias -a

alias art='php artisan '
alias dumpa='composer dump-autoload'
alias rl='route:list'
alias rl='route:list --compact'
alias nrw='npm run watch'
alias nrp='npm run production'
alias nrd='npm run development'

function dexec() {
    docker-compose exec -it "$1" /bin/sh
}

cd /var/www/html

clear
